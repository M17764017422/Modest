name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - run: checkinstall
    - name: make
      run: make
    - name: make test
      run: make test
    - name: make 打包
      run: make make-pc-file
    - name: make install
      run: sudo make install
